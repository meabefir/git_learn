git branch -> shows the branch you are on
git branch <name> -> creates a branch from the current branch you are on. it is named "name"
git checkout <name> -> switched to another branch "name"
git status -> to see new and untracked files